name: VA1 core (do CLI)

on:
  pull_request:
    paths:
      - "package.json"
      - "package-lock.json"
      - "uDosConnect.code-workspace"
      - "core/**"
      - "core-rs/**"
      - "tools/sonic-express/**"
      - "tools/usxd-express/**"
      - "modules/**"
      - "launcher/**"
      - "scripts/check-workspace-drift.sh"
      - "templates/usxd/**"
      - ".github/workflows/core-ci.yml"
  push:
    branches:
      - main
    paths:
      - "package.json"
      - "package-lock.json"
      - "uDosConnect.code-workspace"
      - "core/**"
      - "core-rs/**"
      - "tools/sonic-express/**"
      - "tools/usxd-express/**"
      - "modules/**"
      - "launcher/**"
      - "scripts/check-workspace-drift.sh"
      - "templates/usxd/**"
      - ".github/workflows/core-ci.yml"

jobs:
  workspace:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: package-lock.json
      - name: Workspace drift guard
        run: npm run workspace:drift
      - name: Install workspace and test
        run: npm ci && npm test
