# Network Features (A2)

Status: STUB -- Deferred to A2

A2 Network Features:
- Cloud sync (vault to cloud)
- Multi-device sync
- P2P mesh (LoRa, BLE)
- Webhook receivers
- Real-time events (WebSocket)
- OAuth2 provider

A1 Stubs:
- `do sync pull/push` -> calls A2 if configured
- `do workflow webhook` -> queues for A2
- `do beacon scan` -> discovers A2 servers on LAN (future)
